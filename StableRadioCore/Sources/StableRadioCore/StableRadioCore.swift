import Foundation

// This file serves as the main module entry point
// All public types are exported from their respective files

// Models
// - TransmissionFormat, AudioCodec, SampleRate, BitDepth, ChannelConfig (TransmissionFormat.swift)
// - AudioPacket, PacketType (AudioPacket.swift)
// - DeviceInfo, ConnectionStatus, StreamingStats (DeviceInfo.swift)

// Network
// - UDPSocket, UDPListener (UDPSocket.swift)
// - BonjourPublisher, BonjourBrowser (BonjourService.swift)

// Audio
// - AudioFormatConfig, AudioConverter (AudioFormat.swift)
// - AudioCodecProtocol, PCMCodec, IMADPCMCodec, CodecFactory (AudioCodec.swift)

// Buffer
// - RingBuffer, PacketRingBuffer (RingBuffer.swift)
